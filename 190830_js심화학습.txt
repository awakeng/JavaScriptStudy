학습 내용

1. js 특징
    - 객체 기반 언어라고도 함 
    - 표기로 객체 생성도 가능, 속성(변수) 추가, 수정
    - js도 상속 관계
        : 무조건 상위 클래스가 존재, 내부적으로 다수의 함수 제공
    - new 표기 없이도 {} 등의 표기만으로 객체로 생성 

2. 문법
    1. 객체 리터럴 방식
        1. 변수 = {key : "string", key2:숫자}
        2. 변수 = {};
            변수.key = 값

    2. 생성자 방식
        1. Object() 사용


3. 브라우저 기능 활용하면서 개발 


4. 새로운 키워드
    1. 변수 선언시 적용하는 키워드
        1. var - 이미 존재했던 키워드
        2. let
            - 변수의 사용 scope를 명확하게 블록({}) 기반으로 제한을 두는 명시적인 선언


-  js를 이해하기 위해 비교하는 자바 소스
void m(){
    if(true){
        int no = 10;
    }
    S.o.p(no);// 사용 불가
}




* 용어
1. property
    - 객체 내부에 선어되는 속성
    - 자바 관점에서는 변수

2. 생성자 함수로 인스턴스 생성



